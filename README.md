# bank-management-system-java
A console-based Bank Management System built using Java, Hibernate (JPA), and PostgreSQL. It supports CRUD operations for Bank, Branch, Customer, Account, and Loan with proper entity relationships.
